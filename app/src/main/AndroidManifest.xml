<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polytech.communicationpolytech">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".PolytechActivity" />
        <!-- android:theme="@style/HomeTheme" -->

        <activity android:name=".ReseauActivity" />
        <activity android:name=".ReservedSpaceActivity" />
        <activity
            android:name=".ContactActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".CandidatDialog" />
        <activity android:name=".CandidatActivity" />
        <activity android:name=".VideoViewerActivity"
            android:theme="@style/VideoViewerTheme"></activity>
    </application>

</manifest>